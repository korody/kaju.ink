$('.attachments-uploads').hide().fadeToggle(1500).html("<%= j render 'form', job_id: @job.id %>"); 

var attachments = $(".attachments-uploads");

$('html,body').animate({
  scrollTop: attachments.offset().top - 145 },
2000);